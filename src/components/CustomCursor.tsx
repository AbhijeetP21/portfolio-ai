'use client';

// Intentionally renders nothing. The custom cursor was removed in the redesign,
// but the writing pages still import this component, so it stays as a no-op
// to keep those pages untouched.
export function CustomCursor() {
  return null;
}
